apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-service
  namespace: sdsa
spec:
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
    - name: amqp-ssl
      port: 5671
      targetPort: 5671
